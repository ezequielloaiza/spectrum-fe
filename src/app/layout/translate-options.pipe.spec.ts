import { TranslateOptionsPipe } from './translate-options.pipe';

describe('TranslateOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
